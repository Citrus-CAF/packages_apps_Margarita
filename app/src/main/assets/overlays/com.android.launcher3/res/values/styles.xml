<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launcher theme -->
    <style name="BaseLauncherTheme" parent="@*android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorEdgeEffect">#FF757575</item>
    </style>
    <style name="Theme.Light.CustomOverscroll" parent="@*android:style/Theme.Material" />
    <style name="Theme.Dark.CustomOverscroll" parent="@*android:style/Theme.Material" />
</resources>
